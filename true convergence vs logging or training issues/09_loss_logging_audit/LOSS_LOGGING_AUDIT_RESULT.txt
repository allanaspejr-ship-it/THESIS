Code inspection shows the script defines save_training_history(history, plant), which writes history.history to an Excel file when training is executed. However, no raw training-history Excel files are present in the current metadata/training_validation_loss directory. This means the flat loss plots can be visually reviewed from the archive, but exact epoch-by-epoch logging cannot be numerically audited unless the training history files are regenerated or recovered.
